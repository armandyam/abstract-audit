# Core pipeline. Versions are the ones the published figures were built with.
# textstat in particular is pinned exactly: its formula implementations have
# changed between releases, so an unpinned install can shift every readability
# series by a fraction of a point.
pandas==3.0.2
numpy==2.4.4
matplotlib==3.10.8
textstat==0.7.13
spacy==3.8.13

# The spaCy model is a separate download and its version is part of the
# result: parse depth, NP density, passive rate, TTR and the noun/verb counts
# all come from this parser.
#   python -m spacy download en_core_web_sm    # en_core_web_sm==3.8.0

# Only needed to re-score abstracts with the LLM judges (src/metrics/llm_scores.py).
# The pipeline does not import these; the judge scores are shipped in
# data/per_paper/llm_scores/. Scoring 30,595 abstracts on six models needs a GPU.
#   torch
#   transformers
#   accelerate
#   bitsandbytes
